{
  "name": "incidents-mcp",
  "version": "0.1.0",
  "description": "Demo MCP server — incident management with MCP Lens. Exercises moment-shaped presets for impact, timeline, contributors, and cross-domain combinations (incidents + accounts).",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "bin": {
    "incidents-mcp": "./dist/stdio.js",
    "incidents-mcp-http": "./dist/http.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo 'no tests'",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "start": "pnpm run build && node dist/stdio.js",
    "start:http": "pnpm run build && node dist/http.js",
    "serve:http": "node dist/http.js",
    "dev:http": "tsx --watch src/http.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@mcp-lens/sdk": "workspace:*",
    "@mcp-lens/presets": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
